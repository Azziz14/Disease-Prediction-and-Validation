services:
  - type: web
    name: carepredict-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 120"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: MONGO_URI
        sync: false
